# 灵伴 AI 数字家人官网｜本机运行

## 环境

- Node.js 22.13 或更高版本
- npm

## 启动

在终端进入解压后的项目目录，执行：

```bash
npm install
npm run dev
```

终端出现本地访问地址后，用浏览器打开即可（通常为 `http://localhost:5173`）。

## 停止

在运行网站的终端窗口按 `Control + C`。

## 主要页面文件

- `app/page.tsx`：官网内容与页面结构
- `app/globals.css`：颜色、排版和响应式样式
- `app/layout.tsx`：网站标题及基础布局

